# scripts/

`backup-db.sh` và `restore-db.sh` sẽ được thêm ở **Giai đoạn 7** (Vận hành), sau khi chốt cách backup phù hợp với gói Supabase Free đang dùng — xem `docs/SPEC.md` mục 10 (Q3) và mục 7 GĐ7.

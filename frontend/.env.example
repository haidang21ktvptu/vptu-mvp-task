# Sao chép thành .env (đã bị .gitignore) và điền giá trị của project Supabase.
# CHỈ dùng anon key (public). Không bao giờ đặt service_role key vào đây.
# Staging: vojmrjezspdftovzinek — Production: frwyxcmbonjaimziiuqr (chỉ CI dùng, qua GitHub Secrets).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>

# TRẠNG THÁI DỰ ÁN — bản bàn giao (cập nhật: 2026-09-21, production đang chạy **`v3.6.3`** (20/9: `v3.6.2` sửa Xem diễn biến + tối ưu phân loại CI; `v3.6.3` Giao lại đổi chủ trì, **migration `0045`** — CHANGELOG mục 29). Đặc tả UI/UX mới **`docs/ui-ux/mockup-v8/`** đã chốt 21/9, chờ triển khai.)

Đọc trước: `CLAUDE.md` (quy tắc), **`docs/MUC-TIEU-1400.md` (thước đo mọi giai đoạn)**, `docs/SPEC.md` (v3, nghiệp vụ), `docs/RA-SOAT-HIEN-TRANG.md` (giữ/sửa/hợp nhất/bỏ), `docs/CAU-HOI-NGHIEP-VU.md` (15 câu chờ chủ dự án), `docs/LO-TRINH-V3.md` (GĐ14–24, migration từ 0022), `docs/DESIGN.md` (hệ thống thiết kế) + **`docs/ui-ux/mockup-v8/`** (đặc tả UI/UX hiện hành, `DESIGN-V8.md` + 7 màn hình; `mockup-v7.html` là lịch sử, đã thay bằng v8), `docs/kien-truc.md` (pipeline, secret, quay lui), `docs/PROMPTS.md` (prompt theo giai đoạn). Tra cứu: `docs/SPEC-v2-luu.md` (mã yêu cầu cũ), `docs/thiet-ke-theo-doi-kl-btvtu.md` (thiết kế GĐ8–10, đóng băng). Lịch sử: `CHANGELOG.md` mục 7–20.

## 1. Hiện trạng 7 giai đoạn
| GĐ | Nội dung | Trạng thái |
|---|---|---|
| 0 | Khung thư mục, migration 0001 baseline, CI lint | Xong |
| 1 | Chặn rò rỉ: RLS tạm, `accounts_public`, bỏ đọc/ghi thẳng `password` | Xong |
| 2 | Supabase Auth (giữ mật khẩu cũ, bcrypt tại máy), 0004–0006, `config.toml` | Xong |
| 3 | RLS đầy đủ theo vai trò (0007–0010), hàm nghiệp vụ, `tests/rls` 48 test | Xong |
| 4 | Vite + module hoá (`frontend/`), 0011 FK `accounts.id → auth.users.id`, `tests/e2e` | Xong |
| 5 | Giao diện mới theo DESIGN.md, phông tự host, responsive, in, Lighthouse A11y 100 | Xong |
| 6 | CI/CD: test trên staging trong CI, deploy-staging, deploy-prod có duyệt (tag trước merge), 0012 `is_system` + `smoke_test` | Xong — `v2.0.0-rc2` (2026-09-14), `v2.1.0` (2026-09-15, tag `production` = `5368445`) |
| 7 | Vận hành: backup/restore script, uptime monitor, `docs/xu-ly-su-co.md`, lên Pro + bật hook khoá tài khoản | **Xong** (2026-09-14): PR A #29, PR B #30, PR C; NF-6 đạt (biên bản khôi phục thử). Còn việc ngoài code: lên gói Pro + bật AUTH-3 |
| 8 | Theo dõi KL BTVTU — mô hình dữ liệu và nhập liệu (`docs/thiet-ke-theo-doi-kl-btvtu.md` Phần 4) | **Xong** (2026-09-15): PR 8A-1 #33, 8A-2 #34, 8B #35, phát hành `v2.1.0` #36 (0013–0017); dữ liệu thật đã nhập production (185 nhiệm vụ, 32 hội nghị, 194 nhật ký — `docs/bien-ban-nhap-kl-btvtu.md`), bộ số 14/9 khớp 146/16/8/6/6/3/0; đã cấp `quan_tri_kl` 2 người, phân công PCVP ↔ phòng 5 dòng |
| 9 | Phân công theo lĩnh vực (Phần 4 GĐ9, Phần 5.4) | **Xong** (2026-09-15): PR 9A #38, 9B #39, `v2.2.0` #40 (0018–0020), đọc `.xlsx` #41; ánh xạ lĩnh vực đợt 1 trên production: 9 cặp → 51 dòng, 133 dòng còn NULL (biên bản `docs/bien-ban-nhap-kl-btvtu.md` mục 7) |
| 10 | Màn hình chuyên viên + dashboard lãnh đạo KL, thời gian thực (kế hoạch chốt 15/9, CHANGELOG mục 19) | **Mã xong, đang phát hành** (2026-09-15): 10A #43 (0021 minh chứng bắt buộc), 10B #44 (danh sách KL, cập nhật nhanh, truy vết), 10C #45 (dashboard A1), 10D #46 (realtime + dự phòng), 10F #47 (form nhập nhiệm vụ) đều merged; 10E #48 lỡ merge trước khi tag (`docs/xu-ly-su-co.md` #13) → phát hành lại bằng `feature/phat-hanh-v2.3.0-lan-2` (0021 lên production, cần xác nhận trong phiên; **không merge PR phát hành trước khi có tag `production`**). Xong GĐ10 khi chạy song song 2 kỳ trùng số + ≥ 9 chuyên viên tự cập nhật. Số đo production 15/9 (chỉ đọc): 185 nhiệm vụ, 146 HT, **78 HT thiếu minh chứng**, 146 HT không ngày hoàn thành gốc, **134 dòng `linh_vuc_ma` NULL** (không phải 133 — 133 là trong 184 dòng có `linh_vuc_chi_tiet`; 1 dòng không có chi tiết cũng NULL), 0 dòng thiếu ngành; mốc 14/9 = 146/16/8/6/6/3 |
| 11–13 | GĐ11 vòng chỉ đạo + đính chính UI + tắt Excel; GĐ12 quản trị nhân sự; GĐ13 xuất PDF, bảng chéo, `kl_bao_cao` | **Xếp lại** vào GĐ18–19 của `docs/LO-TRINH-V3.md` (không bỏ) |
| **Định hướng lại (16/9)** | Xương sống từ nay = phụ lục Công văn 1400 (1-1-1-1-3, 5 chức năng MVP, mô hình 4 cấp Thường trực → Văn phòng → phòng → chuyên viên). Module KL không còn là module cạnh hệ thống mà là bước "văn bản đến → tạo nhiệm vụ" của luồng gốc; hợp nhất `tasks` (0 dòng) và `kl_nhiem_vu` (185 dòng) thành một thực thể `nhiem_vu` | **Tài liệu xong, câu hỏi đã chốt** (PR-0 CI #50, PR-1 #51 bộ 5 tài liệu + quyết định 16/9). Khác đề xuất: CH-6 = B (minh chứng có cấu trúc, **không Storage**, tệp chờ kinh phí), CH-11 = A (vai trò `A0` cho 3 tài khoản Thường trực), KT-4 đổi (cảnh báo bằng cron GitHub Actions, **không `pg_cron`**); bỏ điều kiện gói Pro. GĐ14 đã làm theo lộ trình |
| **14** | Thực thể thống nhất (`nhiem_vu` = 185 việc + Owner/Product/ngày nhận/cấp; `giao_viec`, `xac_nhan_nhan_viec`; màn hình A1/A2/A3 dùng chung) | **Xong, `v3.0.0` trên production** (2026-09-16): 14AB #52 (0022–0024), 14C #53 (0025), 14D #54 |
| **15** | **Điều hành ngoại lệ** (= GĐ17 + 18B của lộ trình, **đảo lên** theo phụ lục 1400 bước 5: lãnh đạo ra quyết định trực tiếp trên điểm nghẽn) — `0026`: `chi_dao` loại Y_KIEN/PHAN_HOI + `chi_dao_da_doc`, hàm `chi_dao_gui/phan_hoi/dong`, `nguoi_lien_quan`, tin hệ thống trong `direct_messages`, `dat_cap_quyet_dinh`, view `v_ngoai_le`; Dashboard 4 trường CN-5.2, luồng chỉ đạo trên mọi nhiệm vụ, chuông thông báo | **Xong** (2026-09-16): 15A #56 (0026, RLS 149), 15B #57 (Dashboard + luồng chỉ đạo + chuông + e2e 40; CI e2e 5m20s → 3m25s); **`v3.1.0` trên production** (0026, backup tay `prod-20260916-0531-tay`); **15C #59** giao diện điều hành (khối Chỉ đạo đầu ngăn chi tiết, người dùng đầu thanh bên) + **15D #61** migration `0027` sửa mặc định mã nhiệm vụ (lpad cắt số khi sequence > 999; staging 1001, production 185) → **phát hành `v3.1.1`** (0027 lên production: **có migration → backup tay + xác nhận trong phiên**; quy trình rút gọn từ 16/9: PR chỉ frontend bật auto-merge khi repo cho phép, phát hành gom lô cuối buổi, backup tay chỉ khi có migration). Sau phát hành: kiểm 3 vai trên bản live, đối chiếu 185 / 134 / 78 và 146/16/8/6/6/3 |
| **17–18** | Cảnh báo tự động 3 cấp (0029, `canh-bao.yml`); vai trò `A0` Thường trực + dọn luồng cũ (0030–0031) | **Xong, `v3.4.0`** (2026-09-16): #67, #68 |
| **19** | **Chỉ đạo Thường trực `CHI_DAO_TT`** (CH-16) — `0032`: chỉ A0 gửi, người nhận tự tính (CVP + PCVP phụ trách + lãnh đạo phụ trách đơn vị ngoài), hạn 2 ngày làm việc, chỉ đạo con gắn `tra_loi_cho`, A0 đóng luồng mình, quét quá hạn trong `canh_bao_quet`, view `v_chi_dao_tt`; Dashboard khối "Chỉ đạo Thường trực" đầu trang A1/A0; ô nhập A0 Ý kiến/Chỉ đạo | **Xong (phần MVP 1400), đang phát hành `v3.5.0`** (2026-09-16): #70. Hoãn `v3.6`: PDF hai bản, nhân sự theo ngày, nhập bổ sung dữ liệu, V-Office |
| **16** | **Minh chứng có cấu trúc và đóng nhiệm vụ** (= GĐ15 cũ của lộ trình; MC-2…MC-6, DL-4, CH-6 = B không Storage) — `0028`: bảng `minh_chung`, một vị từ hợp lệ dùng chung, 76 minh chứng chữ → `chu_cu`, `thieu_minh_chung` do trigger tính, hàm `nop_minh_chung/xac_nhan_minh_chung/dong_nhiem_vu` chốt lead time; khối Minh chứng + hộp Nộp/Đóng trong giao diện, nút Đóng theo minh chứng hợp lệ | **Xong, `v3.2.0`** (2026-09-16): 15E #62, CI-4 #63, 16A #64 (0028), 16B #65 |
| **20–24** | **Giao diện v7 + Trung tâm điều hành A0 + độ khẩn/giao thay mặt/từ chối có duyệt + quản trị hệ thống** — GĐ20 #73 (0033: giao diện v7 theo mockup, token màu/chữ, Be Vietnam Pro tự chứa; A0 Trung tâm điều hành 4 số-lọc có xu hướng, thanh trái khâu/đơn vị; `v_ngoai_le.khau`); GĐ21 #74 (0034: từ chối nhận việc có duyệt — A3 đề nghị, lãnh đạo trực tiếp duyệt, A0 duyệt cấp Thường trực; A0 Cán bộ + Nhắn tin; bộ lọc một hàng); GĐ22 #75 (0035–0037: độ khẩn 4 cấp Thường/Khẩn/Thượng khẩn/Hoả tốc, giao thay mặt, Thường trực giao việc trên biểu mẫu rút gọn, diễn biến tại chỗ `v_dien_bien`, cảnh báo TT chưa nhận/hoả tốc, công tắc kiểm thử `KIEM_THU_MOI_TRUONG`); GĐ23 #76 (0038–0042: dải nhận diện + menu bánh răng theo vai, đổi mật khẩu lần đầu, **Edge Function `quan-tri-tai-khoan`** (tạo/khoá/đặt mật khẩu tạm bằng service_role trong function, nhật ký `nhat_ky_he_thong`), **Dọn dữ liệu hai bước** (xem trước → gõ `XOÁ`, chỉ khi mốc backup < 24 giờ), mốc backup, bản tin 7h30, nạp lại đúng việc sau mọi hành động ghi); GĐ24 #77–#79 (hộp thả xuống chuông/bánh răng; **dải "Cần xử lý ngay" có nút hành động dẫn tới việc**; 0043–0044 bộ sẵn dọn dữ liệu thử; **bộ `E2E-SEED` + phòng thử `E2E_PT`** để e2e chạy trên DB rỗng; vẽ lại thẻ A3/dòng Nhiệm vụ ngay sau ghi; kiểm phạm vi việc mẫu ngay ở beforeAll) | **Xong, phát hành `v3.6.0`** (2026-09-17): migration `0033`–`0044` đã trên production; backup `prod-20260917-1605-truoc-v3.6.0`. **`v3.6.1` (18/9/2026, #81 #82, không migration)**: nhãn độ khẩn thẻ nghẽn (`.the .ten > span`, `.dai .ten`), nhãn "Việc Đỏ chờ xác nhận minh chứng", việc mới chờ xác nhận theo từng người (`toi_da_xac_nhan`), spec `viec-moi-tung-nguoi`; `backup-dinh-ky.yml` hết exit 127 (Backup định kỳ #7 xanh) |

## 2. Kiến trúc (tóm tắt)
- Frontend Vite + JS thuần + Tailwind build, `frontend/src/{auth,lib,views/{a1,a2,a3,shared},features/{directives,messages,tasks},components,styles}`; anon key qua `VITE_SUPABASE_*`; `base` = `/vptu-mvp-task/` (staging: `/vptu-mvp-task/staging/`).
- Backend chỉ Supabase: Auth (email quy ước `<username>@vptu.caobang.local`), Postgres + RLS + hàm `security definer` (`giao_viec`, `xac_nhan_nhan_viec`, `kl_*`, `admin_*`, `mark_messages_read`; `assign_task`/`approve_task`/`submit_evidence`/`warn_task` còn trong DB tới 18A, frontend không gọi), Realtime `postgres_changes`. Schema = `supabase/migrations/0001–0044` (production đã ở 0044 từ CI kiểm thử trên production 17/9; staging quá tải, không dùng từ 17/9; dữ liệu KL trên production: 185 nhiệm vụ, 51 dòng đã có `linh_vuc_ma`), không sửa tay Dashboard. Migration kế tiếp đánh số từ **0028** (GĐ16 minh chứng có cấu trúc). Hàm điều hành ngoại lệ (0026): `chi_dao_gui/phan_hoi/dong`, `nguoi_lien_quan`, `dat_cap_quyet_dinh`, `tin_he_thong_da_doc`, `chi_dao_danh_dau_doc`; view `v_ngoai_le`. Bí danh `kl_nhiem_vu`/`kl_hoi_nghi`/`kl_lich_su`/`kl_chi_dao`/`kl_dinh_chinh`/`dm_co_quan_trinh`/`v_kl_dashboard`/`kl_trang_thai` và bảng `tasks*` giữ tới 18A.
- Project: **production** `frwyxcmbonjaimziiuqr` (48 cán bộ thật + `smoke_test`; module KL: 185 nhiệm vụ thật `nguon = excel`, 2 tài khoản `quan_tri_kl`, 5 dòng `phu_trach_phong`), **staging** `vojmrjezspdftovzinek` (8 tài khoản giả từ `seed.sql`, mật khẩu `123456`; `demo_qtht` giữ `quan_tri_he_thong`; module KL: bộ dữ liệu vàng ẩn danh 185 dòng từ `tests/rls/du-lieu-vang/kl-btvtu.json`). CLI local luôn link staging; production dùng `--project-ref`.
- Hosting: một site GitHub Pages chứa 2 bản — `/` production (build từ tag `production`; chưa có tag thì từ `main`), `/staging/` (build từ `main`). `phien-ban.json` ở mỗi bản ghi ref/commit.
- Pipeline (rút gọn PR #50, 16/9): `ci.yml` (PR: gitleaks, phân loại PR, migration+lint+RLS local, build+lint+300 dòng, e2e trên staging — 8 lượt đăng nhập/lần; **phân loại thay đổi bằng `.github/scripts/phan-loai.sh` (18/9): chỉ `docs/**`/`*.md`/workflow backup, cảnh báo → ba job chậm thay bằng job rỗng cùng tên, kể cả PR release; concurrency huỷ lượt PR cũ cùng nhánh** — bảng ở `docs/KIEM-THU.md`; Chromium cache) → `deploy-staging.yml` (push main, **bỏ qua cả run khi thay đổi không ảnh hưởng app**: db push staging → RLS token thật ‖ build 2 bản → Pages) → `deploy-prod.yml` (tag `v*` trên đầu nhánh phát hành: `kiem-tra` → duyệt → backup gpg → db push production → build → Pages → smoke `smoke_test` → gắn tag `production`). GitHub Pages chỉ nhận mỗi commit một lần (`pages_build_version` = sha OIDC) — vì thế tag phải gắn trước khi merge.

## 3. Secret và environment (GitHub → Settings)
| Nơi | Tên | Ghi chú |
|---|---|---|
| Repository | `SUPABASE_ACCESS_TOKEN` | token cá nhân Supabase, **hết hạn sau 90 ngày** — xem mục 5 |
| Repository | `STAGING_DB_PASSWORD`, `STAGING_SUPABASE_ANON_KEY`, `STAGING_SUPABASE_SERVICE_ROLE_KEY` | chỉ staging; service_role dùng dọn dữ liệu test |
| Repository | `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` | production, chỉ anon key |
| Repository | `SUPABASE_SERVICE_ROLE_KEY`, `STAGING_SERVICE_ROLE_KEY` | workflow *Cảnh báo tự động* (`canh-bao.yml`, GĐ17) gọi `canh_bao_quet()` bằng service_role trên production / staging; không bao giờ vào frontend |
| Repository | `SMOKE_USERNAME` (= `smoke_test`), `SMOKE_PASSWORD` | tài khoản hệ thống production, `is_system = true` |
| Repository | `BACKUP_PASSPHRASE` | cùng giá trị với environment `production`; cho `backup-dinh-ky.yml` (workflow tự động không dùng environment có reviewer được) |
| Environment `production` | `PROD_DB_PASSWORD`, `BACKUP_PASSPHRASE` | reviewer `haidang21ktvptu`, chỉ tag `v*`; passphrase giải mã backup — mất là mất backup |
| Environment `github-pages` | (không secret) | cho phép `main` + tag `v*`, không reviewer |
Ruleset `main`: PR bắt buộc, chặn force-push, 4 check bắt buộc: `Quét rò rỉ bí mật`, `Áp migration + lint schema`, `Build frontend + giới hạn 300 dòng`, `Kiểm thử RLS + e2e trên staging`.

## 4. Quyết định đã chốt (không mở lại)
- Vai trò tầng DB: CVP = A1 + `is_chief`; PCVP = A1 còn lại, khối tính 2 cấp qua `manager_id`; A2 theo `department`; A3 chỉ việc của mình; A2 đọc/ghi luồng ý kiến mọi task trong phòng (0010).
- Giữ mật khẩu cũ khi chuyển sang Auth; `must_change_password` bật tay bằng `admin_set_must_change_password()`. Hook khoá 15 phút (AUTH-3) tạm tắt vì gói Free — bật ở GĐ7 khi lên Pro.
- Staging chỉ dữ liệu giả từ `seed.sql` (rule 11). Mọi migration production cần xác nhận trong phiên (rule 12); từ GĐ6 = duyệt environment `production`. `supabase config push` vẫn làm tay sau khi trình `config diff` (kỳ vọng: `site_url` GitHub Pages, MFA/OTP tắt).
- Không merge thẳng `main`, kể cả docs; chỉ báo "mời merge" sau khi CI xanh trên commit cuối; chủ dự án tự merge.
- GĐ6: phương án A (`/staging/` cùng artifact Pages); tài khoản hệ thống `smoke_test` (A3, CDS_CY, ẩn khỏi mọi danh sách qua `loadAccountsCache`); `SMOKE_*` ở repository secret; RLS chạy thêm trên Supabase local trong CI; production = tag `v*`. **Tag `v*` gắn lên commit đầu nhánh phát hành khi PR đã CI xanh, TRƯỚC khi merge; merge bằng merge commit** (GitHub Pages chỉ nhận mỗi commit một lần — kết luận H1 sau sự cố rc1); quay lui = nhánh mới từ commit cũ + commit rỗng + tag mới (không tag lại commit cũ).
- GĐ8 (15/9): phát hành schema (tag) và nhập dữ liệu thật là hai bước tách biệt — script nhập chạy tay từ máy chủ dự án, có backup, xác nhận trong phiên, không nằm trong pipeline; bộ vàng ẩn danh vào local/CI/staging bằng chính script nhập; danh mục đối chiếu nguyên văn, không bí danh.
- Giữ toast thay `alert()`; không file nào trên 300 dòng; giao diện tiếng Việt có dấu; DESIGN.md là nguồn duy nhất về màu/phông/bố cục.
- **Xác nhận nhận việc (17/9, `v3.6.1`)**: chủ trì (Owner tài khoản) và người theo dõi **mỗi người tự bấm "Xác nhận đã nhận"**, không nhận thay nhau — dải "Cần xử lý ngay", thẻ "Việc mới giao", nút ở ngăn chi tiết và `kl_so_chua_xu_ly.viec_moi` tính theo chính người xem (`toi_da_xac_nhan`); khâu nghẽn "Chưa nhận việc" và cảnh báo tự động (`da_nhan_viec`, `v_ngoai_le`) tính khi **chưa ai** nhận.

## 5. Việc định kỳ
- **Đầu tháng 12/2026: tạo `SUPABASE_ACCESS_TOKEN` mới và cập nhật secret TRƯỚC khi hết hạn (~2026-12-13, 90 ngày kể từ 2026-09-14)** — làm theo `docs/xu-ly-su-co.md` #11; nếu đã hết hạn (mọi workflow lỗi xác thực) → #7.
- Khi có sự cố: mở `docs/xu-ly-su-co.md` (11 tình huống, lệnh copy-paste) trước khi làm gì khác.
- Backup: mỗi lần phát hành `deploy-prod` tự `pg_dump` trước `db push` (artifact `prod-<ngày>-<tag>.tar.gz.gpg`, 90 ngày) — **không có bước backup tay trước khi tag**; *Backup định kỳ production* giữ lịch 3 ngày/lần; bản local (tuỳ chọn, ngoài GitHub) = `bash scripts/backup-db.sh --project-ref frwyxcmbonjaimziiuqr --thu-muc "/d/TU 2026/Project/vptu-backup"` chạy trong Git Bash (Docker Desktop mở). Chu kỳ tự động **3 ngày/lần** (PR B) → chấp nhận mất tối đa 3 ngày dữ liệu. Khôi phục: `docs/sao-luu-khoi-phuc.md`.
- Sau mỗi lần phát hành: kiểm tra bản live bằng 3 vai trò (A1 `levanmieu`, A2 `macthuylinh`, A3 `buibahaidang`); ghi CHANGELOG 3–6 dòng.
- Test trên staging (CI hoặc tay) tối đa ~2 lần/5 phút (giới hạn 30 lượt đăng nhập/IP).
- **Backup định kỳ**: Actions → *Backup định kỳ production* phải có run mới mỗi ≤ 3 ngày; GitHub tự tắt schedule sau 60 ngày repo không có commit → bấm *Enable workflow*. Trên máy: `vptu-backup	ai-backup.log` có dòng mới mỗi lần đăng nhập Windows (Task Scheduler `VPTU tai backup`); UptimeRobot theo dõi `phien-ban.json` bản live (docs/sao-luu-khoi-phuc.md mục 3, 9). Đã đăng ký cả hai ngày 2026-09-14; hai secret `BACKUP_PASSPHRASE` (repository + environment) đã kiểm chứng trùng nhau (giải mã artifact CI bằng passphrase trong trình quản lý mật khẩu).
- `supabase/setup-cli@v1` có thể lỗi "rate limit exceeded" (tải CLI từ GitHub releases) khi chạy nhiều workflow trong ngày — chờ 30–60 phút rồi *Re-run failed jobs*; không phải lỗi cấu hình.
- Repo là public — không commit file `.xlsx`/`.pdf` chứa dữ liệu thật, không dán ảnh dashboard có tên thật vào PR/issue.
- **Cảnh báo tự động** (GĐ17): Actions → *Cảnh báo tự động* phải có run mới mỗi sáng (cron 07:30 giờ Việt Nam); GitHub tự tắt schedule sau 60 ngày repo không có commit → bấm *Enable workflow*; run lỗi thì mở `xu-ly-su-co.md` (mục cảnh báo) và chạy tay *Run workflow*.

## 6. Việc còn lại
0. **Kế tiếp (sau `v3.6.3`)**: **triển khai giao diện v8** theo `docs/ui-ux/mockup-v8/` — **đợt 1 (khung: token, Lora, thanh đầu trang 72/56px, menu dọc 236px + ngăn kéo, nút/ô nhập/thẻ) đã làm** (PR `feature/giao-dien-v8-dot-1`, CHANGELOG mục 31; e2e `bo-cuc-mobile` + `a0` xanh trên production); **đợt 2 đã làm** (PR `feature/giao-dien-v8-dot-2`, CHANGELOG mục 32): đăng nhập (01) + 4 màn hình chính theo vai (02–05) ba tầng, hàng việc; **đợt 3 đã làm** (PR `feature/giao-dien-v8-dot-3`, CHANGELOG mục 33): tab Nhiệm vụ (06) + Giao việc (07), 6 chỉnh sau kiểm tay, RLS staging chỉ chạy khi chạm supabase/tests/rls; **còn lại**: thay ảnh Bản Giốc tạm bằng ảnh có bản quyền (`frontend/public/brand/README.md`), rà lại 7 màn hình trên staging; chỉ đổi lớp trình bày, giữ hành vi/`#klBody[data-nap]`/id vùng e2e; tách PR theo màn hình, mỗi file ≤300 dòng; tắt `vars.KIEM_THU_MOI_TRUONG` hoặc giữ production làm môi trường kiểm thử chính (staging quá tải — quyết định 17/9) và dọn dữ liệu thử trước go-live bằng bộ sẵn "dữ liệu thử" (xoá `NV-T*`, `E2E-TEST*`, `E2E-SEED*`, `demo_*`, phòng thử `E2E_*`); bật cờ đổi mật khẩu lần đầu cho tài khoản thật (`scripts/bat-co-doi-mat-khau.mjs --thuc-hien`); các hạng mục CV-2 hoãn từ `v3.5.0` (xuất HTML/PDF hai bản, quản trị nhân sự hiệu lực theo ngày, nhập bổ sung dữ liệu chuyển đổi CH-13, đặc tả V-Office) và 18C (đính chính có UI, tắt sheet) vẫn chờ. **GĐ19 (v3.5.0)** và **GĐ20–24 (v3.6.0)** xong — xem bảng mục 1.
1. **GĐ10 — đã phát hành `v2.3.0`** (#49, 0021 trên production; sự cố #48 ghi `xu-ly-su-co.md` #13). Quyết định đã chốt của GĐ10 giữ hiệu lực: minh chứng dạng văn bản (tệp → GĐ15); bỏ "so với kỳ trước"; hàng 4 = ngành → lĩnh vực; chủ trì không đổi loại thời hạn; số liệu đếm phía client từ dòng RLS trả về; 134 dòng lĩnh vực NULL chỉ `quan_tri_kl`/script/đính chính điền. Số đo production 15/9: 185 nhiệm vụ, 146 HT (146 không ngày hoàn thành gốc, 78 thiếu minh chứng), 65 "Có hạn cụ thể" không hạn (62 đã xong + 3 mở có lý do), 76 minh chứng dạng chữ, 29 việc cơ quan trình = Văn phòng, `kl_lich_su` `nguon = app` 51 dòng đều do script (0 chuyên viên tự cập nhật).
2. **Ánh xạ lĩnh vực đợt 2+ (việc định kỳ cho tới khi hết)**: còn 14 cặp có gợi ý "chứa tên" + 61 cặp trống (133 dòng) trên production. Khi người quản trị sheet duyệt thêm trên file Excel (`vptu-backup/nguon-kl-btvtu/anh-xa-linh-vuc.2026-09-15.xlsx`, cột E): backup → `node scripts/anh-xa-linh-vuc.mjs --project-ref frwyxcmbonjaimziiuqr --production --ghi --file "<xlsx>"` (xác nhận trong phiên) — script **bỏ qua dòng đã có lĩnh vực**, chỉ điền dòng NULL, không đổi `cap_nhat_luc`; bổ sung kết quả vào biên bản mục 7 qua PR. Muốn xuất lại bảng duyệt (chỉ đọc): thêm `--out "<file>.xlsx"` — dòng đã có ghi chú "đã có: <mã>". Việc mới nhập trên app (GĐ10 form) chọn lĩnh vực bắt buộc nên không phát sinh thêm.
3. **GĐ11–13 cũ** (vòng chỉ đạo + tắt Excel; quản trị nhân sự; xuất PDF, bảng chéo) — **xếp lại** thành GĐ18 (chỉ đạo đầy đủ, đính chính UI, tắt sheet) và GĐ19 (xuất PDF hai bản, nhân sự, nhập bổ sung dữ liệu chuyển đổi, chuẩn bị V-Office) trong `LO-TRINH-V3.md`; nội dung không bỏ.
4. **GĐ14–18 (thứ tự đã đảo 16/9 so với `LO-TRINH-V3.md`)**: 14 thực thể thống nhất (0022–0025, `v3.0.0` — **xong**); **15 điều hành ngoại lệ** (0026, `v3.1.0` — **xong**, = GĐ17 + 18B cũ); **16 minh chứng có cấu trúc + đóng nhiệm vụ** (0028, không tệp, `v3.2.0` — **xong**); **17 cảnh báo tự động 3 cấp** bằng workflow cron GitHub (0029 — **xong** #67, gộp vào `v3.4.0`, không phát hành `v3.3.0` riêng); **18 vai trò `A0` Thường trực + dọn luồng cũ** (0030–0031, `v3.4.0` — **xong** #68; đính chính UI + tắt sheet chuyển sang GĐ19); **19 chỉ đạo Thường trực `CHI_DAO_TT`** (0032, `v3.5.0` — **xong phần MVP** #70; 4 hạng mục CV-2 hoãn `v3.6`). Lý do đảo: phụ lục 1400 bước 5 — lãnh đạo mở dashboard ngoại lệ và ra quyết định trực tiếp vào điểm nghẽn trước, minh chứng/cảnh báo tự động lùi sau. Cập nhật `LO-TRINH-V3.md` theo thứ tự này ở PR tài liệu kế tiếp.
5. **Sổ CI**: job `kiem-thu-staging` từ 5m20s xuống 3m25s (#57: offline/online tức thì trong `kl-realtime`, mobile chỉ 4 spec nhạy bố cục). Việc còn lại để rút thêm: **2 worker Playwright** (tách dữ liệu mẫu theo project để chạy song song desktop/mobile; ngân sách đăng nhập 30 lượt/5 phút vẫn đủ vì phiên dùng storageState).
6. ~~Đổi ba mặc định `current_date` trong 0013 sang `kl_hom_nay()`~~ — **đã làm trong 0018 (PR 9A)**: `phu_trach(p_ngay)`, `admin_phan_cong_phong(p_tu_ngay)`, cột `phu_trach_phong.tu_ngay`; `rls-11` kiểm mặc định = `kl_hom_nay()`.
7. **Áp phạm vi PCVP theo `phu_trach_phong` cho phần giao việc nội bộ** (tasks/RLS GĐ3) — PR riêng sau GĐ8, vì GĐ8 chỉ áp cho module KL (quyết định 7 trong tài liệu thiết kế).
8. **GĐ7 xong (2026-09-14)** — PR A #29 (`backup-db.sh`, `restore-db.sh`, biên bản khôi phục thử trên local), PR B #30 (`backup-dinh-ky.yml` 3 ngày/lần, `tai-backup.sh` + Task Scheduler, UptimeRobot), PR C (`docs/xu-ly-su-co.md` 11 tình huống). **Còn việc ngoài code:** lên gói Supabase Pro → bật hook khoá tài khoản AUTH-3 (`supabase/config.toml` `[auth.hook.password_verification_attempt] enabled = true`, `config diff` → `config push`, cần xác nhận trong phiên) → diễn tập lại khôi phục trên project hosted tạm (`docs/sao-luu-khoi-phuc.md` mục 5) → biên bản mới.
9. Sau mỗi phát hành: kiểm tra bản live 3 vai trò, CHANGELOG 3–6 dòng (mục 5).

## 7. Lệnh để tiếp tục
```
cd tests/e2e && npm test      # 40 test e2e trên staging (~11 lượt đăng nhập, mobile chỉ 4 spec); cd tests/rls && npm test (149 test local, 7 lượt); cd frontend && npm test (14 unit); RLS_LOCAL=1 / E2E_LOCAL=1 để chạy trên Supabase local; npm run lint ở gốc
```
Phát hành production (chi tiết + cảnh báo: `docs/kien-truc.md` mục 7) — tag TRƯỚC khi merge, trên đầu nhánh của PR đã CI xanh:
```
git checkout feature/ten-nhanh && git pull && gh pr checks     # đúng nhánh phát hành, 4 check xanh (PR chỉ tài liệu: job rỗng cùng tên vẫn được chấp nhận); KHÔNG cần backup tay — deploy-prod tự pg_dump trước db push
git tag v2.x.y && git push origin v2.x.y                        # → deploy-prod: kiem-tra → Review deployments → smoke → tag production
# duyệt trên GitHub, đợi 4 job xanh, rồi merge PR bằng merge commit; không push thêm commit lên nhánh sau khi tag
```
Phiên kế tiếp: mục 6.0 — sau khi tag `v3.6.1` và merge PR release: kiểm bản live 3 vai + A0 (nhãn độ khẩn thẻ nghẽn, dải "Cần xử lý ngay" theo từng người); quyết định môi trường kiểm thử (production/staging) và dọn dữ liệu thử trước go-live.
